<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <!-- Gmail app query -->
        <package android:name="com.google.android.gm" />
        <!-- Outlook app query -->
        <package android:name="com.microsoft.office.outlook" />
        <!-- Mailto intents -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Link Handler for QR Codes (URL directa y Custom Scheme) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- https://reportesexpress.app/maquina?serie=XXXXX o /maquina/XXXXX -->
                <data
                    android:scheme="https"
                    android:host="reportesexpress.app"
                    android:pathPrefix="/maquina" />

                <data
                    android:scheme="http"
                    android:host="reportesexpress.app"
                    android:pathPrefix="/maquina" />

                <!-- Esquema directo reportesexpress://maquina?serie=XXXXX -->
                <data
                    android:scheme="reportesexpress"
                    android:host="maquina" />
            </intent-filter>
        </activity>
    </application>

</manifest>
